server:
  port: 8081
  servlet:
    context-path: /api
management:
  endpoint:
    health:
      show-details: always
spring:
  datasource:
    url:  ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
